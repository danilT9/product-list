{
  "name": "product-list",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/html/index.html",
    "build": "parcel build ./src/* --no-cache --public-url /parcel-project/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "parcel": "^2.16.0"
  }
}
